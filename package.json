{
  "name": "omp-gateway",
  "version": "0.1.0",
  "description": "Resident gateway daemon for omp: cron scheduler + official QQ Bot API v2 gateway, driven by `omp --mode rpc`. Full TypeScript/Bun stack.",
  "type": "module",
  "bin": {
    "omp-gateway": "./src/index.ts"
  },
  "files": [
    "src",
    "README.md",
    "CHANGELOG.md"
  ],
  "scripts": {
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "dev": "bun --watch src/index.ts",
    "build": "bun build --compile src/index.ts --outfile dist/omp-gateway"
  },
  "omp": {
    "extensions": ["./src/extension/index.ts"]
  },
  "dependencies": {
    "commander": "^12.0.0",
    "croner": "^9.0.0",
    "yaml": "^2.6.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@oh-my-pi/pi-coding-agent": "17.4.0",
    "@types/bun": "latest",
    "typescript": "^5.7.0"
  }
}
